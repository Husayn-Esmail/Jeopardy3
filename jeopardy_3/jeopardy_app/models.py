from django.db import models

# Create your models here.
class Jeopardy_Question(models.Model):
    """
    Tasks:
    construct the model in such a way that it adapts the
    read_questions.py method. should still have the ability
    to use a CSV to create questions
    """
    pass