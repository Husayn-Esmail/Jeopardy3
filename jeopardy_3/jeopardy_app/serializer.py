from rest_framework import serializers


class ReactSerializer(serializers.ModelSerializer):
    class Meta:
        """
        import the model and set model to it
        then specify the fields to serialize
        """
        pass